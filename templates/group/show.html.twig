{% extends 'base.html.twig' %}

{% block title %}Group{% endblock %}

{% block body %}
    <h1>Group</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ group.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ group.name }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ group.roles ? group.roles|json_encode : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('bisonlab_group_index') }}">back to list</a>

    <a href="{{ path('bisonlab_group_edit', {'id': group.id}) }}">edit</a>

    {{ include('@BisonLabUser/group/_delete_form.html.twig') }}
{% endblock %}
